package com.exalt.training.users.enums;

/**
 * Enum to represent the different subscription or role types for users.
 */
public enum Rule {
    GOLDEN_SUBSCRIPTION, // Premium users with the highest level of privileges
    SHABAB, // Special category for younger users, possibly with specific benefits
    REGULAR // Standard user with basic privileges
}
